{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debugger",
			"type": "node",
			"request": "attach",
			"port": 9229,
			"localRoot": "${workspaceFolder}"
			// "remoteRoot": "/app" # uncomment for docker
		}
	]
}
